# Nixpacks configuration for Coolify deployment
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node ./dist/server.js"

[variables]
NODE_ENV = "production"
